# 버스 배차 스케줄링 - 반복 이완(스프링) 방법

## 프로젝트 개요
버스 배차 시간표를 자동 생성하는 도구. `bus_dispatch_problem_definition.md`에 원본 문제
정의(배차간격, 휴게시간, 이동시간, 슬랙/피크타임, mid_start 자동배정 등 전체 요구사항)가
있으니 가장 먼저 읽을 것.

핵심 알고리즘 아이디어는 "반복 이완(스프링) 방법": 왼쪽(이른 시간)부터 트립을 순서대로
배치하면서, 매번 (1) 이미 배치된 노드들을 하한 기준으로 완전히 압축하고 (2) 목표(배차간격을
중간값에 가깝게)에 맞춰 한 번에 늘리는 과정을 반복한다.

## 파일 구성
- `spring_schedule.py` — 알고리즘 본체 (UI 없음, 순수 로직). 아래 "엔진 구조" 참고.
- `spring_viewer.py` — tkinter 실시간 뷰어. 파라미터를 슬라이더/스테퍼/표로 조절하면
  즉시 재계산해서 간트 차트로 보여줌. `python3 spring_viewer.py`로 실행 (tkinter 필요:
  `apt install python3-tk`).
- `bus_dispatch_problem_definition.md` — 원본 요구사항 문서.

## 아키텍처: 이벤트 그래프 (Simple Temporal Network)
트립(회차) 하나는 최대 3개의 이벤트를 가짐: `S`(시작 정류장 출발), `M`(중간 정류장 도착),
`E`(끝 정류장 도착). 이벤트 사이 관계는 전부 `x_to - x_from ∈ [lo, hi]` 형태의 "엣지"로
표현됨 (`Edge` 클래스, `spring_schedule.py`):

- `leg1`(S→M), `leg2`(M→E): 트립 내부 이동시간
- `start_hw`/`mid_hw`/`end_hw`: 정류장별 배차간격 체인 (세션 내에서만 이어짐, 정류장별로
  범위를 따로 설정 가능 — `headway_start_lo/hi`, `headway_mid_lo/hi`, `headway_end_lo/hi`)
- `rest`: 같은 버스의 휴게시간(점심시간 포함) 체인
- `cap`: day_end/mid_end/morning_start 등 상한 또는 하한 전용 고정 제약

## 엔진 구조
- **엔진 A (다음 노드)**: `build_problem()`에서 결정. mid_start 자동배정(4.6절, 이산 결정)은
  `auto_assign_mid_origin()`으로 전처리에서 먼저 끝내고, 이후 처리 순서는 라운드로빈 순서 +
  트립 내부 S→M→E로 고정 (탐색이 아니라 정해진 순서를 그냥 순회).
- **엔진 B (압축)**: `compact()`. 왼쪽부터, 각 이벤트를 들어오는 모든 엣지의 하한 중
  최댓값으로 배치. 위반(하한이 상한을 넘는 모순)을 만나면 어떤 두 엣지가 충돌했는지
  구조화된 정보로 반환 (9절 진단 요구사항).
- **엔진 C (스트레치)**: `stretch()` + `apply_stretch_and_fix()`. **mid_hw 엣지만** 대상으로
  중간값 쪽으로 늘림 (5절 목표: 균등성은 중간 정류장 기준). day_end/mid_end 여유를 총예산으로
  삼아 [lo,hi] 폭에 비례해서 waterfilling 배분 → 왼쪽부터 실제 적용하며 재검증(다른 엣지의
  상한을 넘으면 clamp) → 시간대 경계를 넘으면 그 지점부터 자동으로 재계산됨.
- **검증**: `verify()`. 모든 엣지를 양방향 재확인, 위반 리스트 반환. `solve()`가 최대 5라운드
  압축↔스트레치를 반복.

## 범용 override 규칙 시스템 (`OverrideRule`)
사용자가 자율적으로 특정 조건의 제약만 조정할 수 있게 만든 범용 규칙:
- `kinds`: 어떤 엣지 종류에 적용할지 (`start_hw`/`mid_hw`/`end_hw`/`leg1`/`leg2`/`rest`)
- 선택자(전부 선택사항, 비우면 "제한 없음"): `bus_from`/`bus_to`(배차간격 체인용 버스쌍),
  `bus`/`occ`(이동시간·휴게시간용, occ는 "하루 전체 회차 번호" global_occ 기준),
  `time_lo`/`time_hi`(특정 시간대에만 적용)
- `lo`/`hi`: 새 값 (None이면 기존 유지)
- 여러 규칙이 매치되면 **더 구체적인 조건의 규칙이 우선**, 구체성이 같으면 나중에 추가된
  규칙이 우선 (`OverrideRule.specificity()` / `resolve_bounds()` 참고)

`suggest_overrides(hard_violations)`가 압축 단계 모순을 분석해서 이 규칙 형식으로 자동
제안을 만들어줌 (단, day_end 같은 `cap` 위반은 배차간격 규칙으로 못 고치므로 제외하고 안내).

## 알려진 이슈 / 다음에 다룰 것
1. **cap(day_end) 위반은 상한 override로 못 고침** — day_end 자체가 상한이라 자기모순.
   실제로 확인된 두 방향의 해법:
   - day_end를 늘리거나 회차 수를 줄이거나 이동시간을 줄임 (구조적 파라미터 변경)
   - 배차간격/휴게시간 **하한을 낮추는** 방향의 override (지금 `suggest_overrides`는
     "상한을 넓히는" 방향만 제안하고 이 방향은 아직 제안 안 함)
2. **위반이 국소적(한 지점)인지 전역적(용량 부족)인지에 따라 해법이 다름**:
   - 국소적(예: 버스5→6 구간만 문제) → 그 지점만 override하는 게 맞음, 분산시키면 오히려 손해
   - 전역적(예: 버스 7대가 하루 안에 회차를 못 채움) → 여러 구간에 걸쳐 완화를 분산시키는 게
     더 합리적. 이걸 하려면 "목표 하한"과 "절대 최소 하한"을 구분하는 개념이 필요함
     (지금은 하한이 목표/최소 역할을 겸하고 있음) — 스트레치 엔진의 반대 방향("압축 엔진")을
     하나 더 만드는 셈. **아직 미구현, 설계만 논의됨.**
3. **day_end를 하드 상한 대신 "목표 + 진짜 한계"로 정규화하는 아이디어** — 지금은 `compact()`가
   위반을 하나라도 반환하면 `solve()`가 스트레치 단계를 건너뛰고 즉시 리턴해버림 (day_end
   위반이 스케줄의 다른 멀쩡한 부분 최적화까지 막아버리는 구조적 결함). day_end_target(목표,
   넘으면 지표로만 표시) / day_end_hard(진짜 못 넘는 한계, 있다면)로 나누는 방향으로 개선 논의
   중. **이것도 아직 미구현, 2번보다 먼저 하는 게 맞다고 판단했었음.**
4. `suggest_overrides()`가 지금은 버스쌍 단위로 뭉쳐서 제안함 (회차마다 초과분이 달라도
   최댓값 하나로 통일). 회차별로 더 정밀하게 쪼갤지는 아직 미결정.
5. 4.9절(직접 이동시간 조작 시 유효성 검사), rest_lo/hi 자동계산(4.2절, 미지정 시 역산)은
   아직 미구현.
6. **미래 계획**: 이 override_rules 시스템 위에 AI 에이전트를 붙여서, 사용자가 자연어로
   요청하면 에이전트가 규칙을 표에 추가/조정하게 만들 계획. 그래서 규칙을 plain dict로
   직렬화 가능하게 설계해둠 — 이 방향으로 확장할 때 이 구조를 최대한 유지할 것.

## 검증 방법
tkinter는 `apt install python3-tk`로 설치. 헤드리스 환경에서 렌더링 확인하려면
`xvfb-run -a python3 spring_viewer.py` + ImageMagick `import -window root out.png`로 스크린샷
가능 (지금까지 이 방식으로 검증해왔음).
