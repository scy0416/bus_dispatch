# 버스 배차 스케줄링 문제 정의서

## 1. 개요

여러 대의 버스가 하나의 고정된 노선(시작 정류장 → 중간 정류장 → 끝 정류장)을
왕복 운행한다. 하루는 **오전 세션**과 **오후 세션**으로 나뉘고, 각 세션마다
버스 한 대가 몇 번의 왕복(회차)을 도는지가 정해져 있다. 이 문서는 "각 회차가
정확히 몇 시에 출발해야 하는가"를 결정하는 문제 자체를 정의한다. (이 문서는
문제 정의만 다루며, 지금까지 어떤 알고리즘/솔버로 풀었는지는 의도적으로
포함하지 않았다 — 다른 방법론으로 접근하기 위함.)

## 2. 기본 구조 및 용어

- **정류장 3곳**: 시작 정류장(start) → 중간 정류장(mid) → 끝 정류장(end).
  일반적인 트립은 시작 정류장에서 출발해 중간 정류장을 거쳐 끝 정류장에
  도착한다.
- **세션**: 오전(morning), 오후(afternoon) 두 개로 나뉘며 각각 독립적으로
  운행 횟수(오전 n_morning회/버스, 오후 m_afternoon회/버스)를 가진다.
- **버스**: n_buses대. 각 버스는 자신의 세션 내에서 여러 번(회차)을 반복
  운행한다.
- **회차(trip/occurrence)**: 한 버스가 한 세션 안에서 몇 번째 왕복인지를
  가리키는 번호.
- **라운드로빈 순서**: 같은 시간대에 여러 버스가 번갈아 출발한다. 전체 순서는
  "버스1의 1회차, 버스2의 1회차, ..., 버스N의 1회차, 버스1의 2회차, ..."
  식으로 버스들이 돌아가며 순서대로 배치된다.
- **이동시간 구간 2개**: 시작→중간 구간(기준값 tau_sm), 중간→끝 구간
  (기준값 tau_me). 특별한 조작이 없는 한 모든 트립은 이 기준값을 그대로
  사용한다.

## 3. 결정 대상 (무엇을 정해야 하는가)

각 회차(각 버스, 각 세션, 각 회차 번호)마다 **그 트립이 실제로 출발하는
시각**(보통은 시작 정류장 출발 시각, 아래 4.7에서 설명할 "중간출발" 트립은
중간 정류장 출발 시각)을 결정해야 한다. 이 출발 시각으로부터 중간 정류장
도착 시각, 끝 정류장 도착 시각이 파생된다(이동시간 구간을 더해서).

## 4. 제약 조건

### 4.1 배차간격 (headway)

같은 정류장을 연속으로 지나가는 두 트립(라운드로빈 순서상 바로 다음 트립)
사이의 시간 간격은 지정된 범위 `[headway_lo, headway_hi]`(분) 안에 있어야
한다. 이 제약은 **세 정류장 각각에 대해 독립적으로** 적용된다:

- **시작 정류장**: 실제로 시작 정류장을 지나가는 트립들(중간출발로 지정되지
  않은 트립들)만의 부분 수열에서, 그 부분 수열상 서로 인접한 트립끼리 적용.
- **중간 정류장**: 모든 트립(시작 출발이든 중간출발이든 상관없이 전부 중간
  정류장은 반드시 지나감)에 대해, 전체 라운드로빈 순서상 인접한 트립끼리 적용.
- **끝 정류장**: 마찬가지로 모든 트립에 대해 전체 순서상 인접한 트립끼리 적용.

### 4.2 휴게시간 (rest time)

**같은 버스**의 연속된 두 트립 사이 — 즉 한 트립을 끝내고 다음 트립을
시작하기까지의 간격 — 은 `[rest_lo, rest_hi]`(분) 범위 안에 있어야 한다.

- 이 범위는 사용자가 직접 지정할 수도 있고, 지정하지 않으면 버스 대수 ·
  배차간격 범위 · 이동시간을 바탕으로 "이 시스템이 구조적으로 소화 가능한
  범위"를 자동으로 계산해서 쓴다(계산이 불가능할 정도로 모순된 조합이면 그
  사실 자체가 문제의 일부로 취급되어야 한다).

### 4.3 오전→오후 연결 ("점심시간")

각 버스의 오후 첫 회차 출발 시각은 다음 두 가지 요구를 동시에 만족해야 한다:

1. 전체 공통의 "오후 목표 시작 시각"(afternoon_target_start) 이후여야 한다.
2. **그 버스 자신의 오전 마지막 회차 종료 시각 + 최소 휴게시간(일반 휴게시간
   하한, 4.2절의 rest_lo)** 이후여야 한다 — 즉 오전에서 오후로 넘어갈 때도
   "쉬는 시간 없이 바로 이어서 출발"하는 일은 없어야 한다.

상한은 기본적으로 없다(오후 목표 시작 시각이 늦으면 그만큼 점심시간이 길어질
수 있음 — 의도된 것). 특정 버스만 점심시간 범위를 정확히 강제하고 싶으면
그 버스의 오전→오후 경계에 대해 별도의 `[lo, hi]` 범위를 지정할 수 있어야
한다(아래 4.8절의 "특정 회차" 방식으로 이 경계 회차를 지정하면 동일한 효과).

### 4.4 하루 운행 마감 시각 (day_end)

지정하면, **모든 버스의 오전/오후 각각 마지막 회차가 끝 정류장에 도착하는
시각**이 이 시각을 넘으면 안 된다. 이것은 **상한**일 뿐이다 — "그 시각까지만
도착하면 됨"이지 정확히 그 시각에 도착해야 하는 게 아니다. 지정하지 않으면
마감 없음.

### 4.5 중간정류장 마감 시각 (mid_end)

day_end와 완전히 같은 성격이지만 기준이 "끝 정류장"이 아니라 "중간 정류장"
이다 — 모든 버스의 오전/오후 각각 마지막 회차가 **중간 정류장에 도착하는
시각**의 상한. 역시 "그 시각까지만 도착하면 됨". day_end와는 독립적인
별도 설정이다(둘 다 지정할 수도, 하나만 지정할 수도, 둘 다 생략할 수도 있음).

### 4.6 중간정류장 목표 서비스 시작 (mid_start) 및 자동 배정

지정하면, 중간 정류장에서의 서비스가 그 시각 즈음부터 시작되어야 한다는
목표다. 일반 트립은 시작 정류장에서 출발해 이동시간(tau_sm)만큼 지나야
중간 정류장에 도착하므로, mid_start가 (오전 시작 시각 + 기본 이동시간)보다
이르면 "공백"이 생긴다. 이 공백을 메우기 위해, **버스 1번부터 순서대로** 몇
대를(공백 크기와 배차간격 목표값을 근거로 계산해서, 최대 버스 총 대수까지)
자동으로 "그 버스의 첫 회차를 중간 정류장에서 곧바로 출발하는 것으로" 바꿔서
mid_start 시각부터 배차간격에 맞춰 서비스가 시작되게 만들어야 한다. 사용자가
이미 수동으로 지정해 둔 버스(아래 4.7절)는 이 자동 배정에서 건드리지 않는다.

### 4.7 중간출발 지정 (수동)

사용자가 "특정 버스의 특정 회차"를 지정하면, 그 회차는 시작 정류장을 거치지
않고 **곧바로 중간 정류장에서 출발**하는 것으로 취급된다. 하루 전체 마지막
회차도 지정 가능하다(뒤에 이어지는 운행이 없어도 상관없는 설정이므로). 이렇게
지정된 트립은:

- 시작 정류장 배차간격 제약(4.1)에서 제외된다(물리적으로 그 정류장에 존재하지
  않으므로).
- 중간/끝 정류장 배차간격, 같은 버스의 휴게시간 제약은 그대로 받되, 이동시간은
  시작→중간 구간이 아예 없고 중간→끝 구간만 적용된다.

### 4.8 특정 회차의 휴게시간 강제 (수동)

- **버스+회차 지정**: "특정 버스의 특정 회차" 뒤의 휴게시간을 정확히 `[lo,
  hi]` 범위로 강제할 수 있다. 하루 전체 마지막 회차는 지정할 수 없다(뒤에
  운행이 없어 휴게시간 개념 자체가 없으므로 — 지정 시 오류로 처리되어야 함).
  지정한 회차가 오전 마지막 회차(즉 오전→오후 경계)와 일치하면 4.3절의
  "점심시간 강제"와 동일한 효과를 낸다.
- **회차 번호만 지정(버스 무관)**: 특정 회차 번호만 지정하면(버스는
  지정하지 않음) **모든 버스에 공통으로** 그 회차 뒤의 휴게시간이 그 범위
  안에 있도록 강제된다. 특정 버스만 다르게 하고 싶으면 위의 "버스+회차 지정"
  방식으로 그 버스만 따로 지정하면 되고, 둘 다 있으면 버스+회차로 구체적으로
  지정한 쪽이 우선한다.

### 4.9 특정 회차 이동시간 직접 조작 (수동)

"특정 버스의 특정 회차의 특정 구간(시작→중간 또는 중간→끝)"을 골라서, 그
구간의 **기준 이동시간 자체**에 정해진 값(델타)을 더하거나 뺄 수 있다. 이는
고정된 변경이다 — 그 트립은 항상 그 조정된 값을 그대로 쓴다(선택의 여지 없이
델타만큼 항상 적용됨. 아래 4.10절의 "범위"와는 다른 개념).

제약:
- 중간출발로 지정된 트립(4.7)에는 "시작→중간" 구간 조작을 적용할 수 없다
  (그 구간 자체가 존재하지 않으므로 — 오류로 처리).
- 조작 결과 이동시간이 음수가 되면 안 된다(오류로 처리).

### 4.10 시간대별 이동시간 변동 ("슬랙타임" / "피크타임")

교통 상황에 따라 실제 이동시간이 달라질 수 있다는 것을 모델링한다 —
출퇴근 시간대(피크타임)에는 길이 막혀 더 오래 걸리고, 차가 적은 시간대
(슬랙타임)에는 더 빨리 갈 수 있다. 이를 위해 다음과 같은 "규칙"을 여러 개
독립적으로 정의할 수 있어야 한다. 각 규칙은:

- **기준(basis)**: "start"(그 트립이 시작 정류장을 출발하는 시각을 기준으로
  판단 — 해당되면 시작→중간 구간에 영향) 또는 "mid"(그 트립이 중간 정류장에
  도착하는 시각을 기준으로 판단 — 해당되면 중간→끝 구간에 영향) 중 하나.
- **시간대(time window)**: 이 규칙이 적용되는 하루 중 특정 시간 범위.
- **슬랙 값(n분)**: 이 시간대에 해당하는 트립은 그 구간 이동시간이 기준값보다
  **최대 n분까지 짧아질 수 있다** (강제로 짧아지는 게 아니라 "그 정도까지
  짧아질 수 있는 여지"임 — 실제로 얼마나 짧아질지는 다른 제약들과의 관계 속에서
  자유롭게 정해질 수 있어야 한다).
- **피크 값(m분)**: 이 시간대에 해당하는 트립은 그 구간 이동시간이 기준값보다
  **최대 m분까지 늘어날 수 있다** (마찬가지로 강제가 아니라 여지).

같은 규칙에 슬랙과 피크를 동시에 줄 수도 있다(그러면 그 구간 이동시간이
[기준값-n, 기준값+m] 범위 안에서 자유로워짐). 여러 규칙을 동시에 정의할 수
있고, 각각 기준/시간대/값을 독립적으로 가질 수 있다. 한 트립이 여러 규칙에
동시에 해당하면(겹치는 시간대 등), 슬랙은 슬랙끼리 가장 큰 값, 피크는
피크끼리 가장 큰 값이 적용된다. 어떤 규칙에도 해당하지 않는 트립은 그냥 기준
이동시간을 고정값으로 그대로 쓴다.

**중요한 차이점**: 이 기능(4.10)은 "어떤 트립이 해당되는지"가 스케줄 자체에
따라 결정되고(그 트립이 실제로 그 시간대에 있어야 적용됨), 적용되면 고정값이
아니라 범위(자유)가 된다. 반면 4.9절의 "직접 조작"은 사용자가 트립을 이름으로
직접 지정하고, 적용되면 고정된 델타만큼 항상 정확히 바뀐다.

## 5. 목표 (무엇이 "좋은" 스케줄인가)

제약을 모두 만족하는 스케줄이 여러 개 있을 수 있는데, 그중에서:

1. **배차간격이 최대한 고르게(균등하게)** 되는 스케줄을 선호한다 — 중간
   정류장 기준으로, 연속 간격이 허용 범위의 하한/상한에 겨우 걸치는 것보다는
   그 범위의 중간값에 가까운 것을 더 선호해야 한다.
2. 배차 균등성이 동등하다면, **전체적으로 가장 이른 시각에 출발하는** 쪽을
   선호한다 — 특별한 이유 없이 스케줄 전체가 통째로 뒤로 밀리는 것은 피해야
   한다.

## 6. 회차 번호 체계

버스+회차를 지정해야 하는 모든 기능(4.7, 4.8, 4.9)에서 회차 번호는 동일한
규칙을 따른다: **그 버스의 하루 전체 운행을 1부터 순서대로 센 것**이다 —
오전 1 ~ n_morning회차, 그 다음 오후 1 ~ m_afternoon회차가 이어서 번호
매겨진다. 예를 들어 n_morning=4인 버스라면 회차 4가 그 버스의 오전 마지막
운행이고, 회차 5가 오후 첫 운행이다. 따라서 회차 4를 지정하면 그 버스의
점심시간(오전→오후 경계)을 가리키는 것과 같다.

## 7. 시간 표기 규칙

모든 시각은 내부적으로 "자정 기준 분(0~1440+)"으로 다루되, 사람이 입력/확인할
때는 "HH:MM" 문자열로 주고받을 수 있어야 한다. 스케줄이 자정을 넘길 수 있으므로
"24:00" 이후("25:30" = 다음날 새벽 1시 30분 등)도 그대로 표기할 수 있어야
한다(24시로 wrap-around 되면 안 됨).

## 8. 기본 파라미터 목록

- `n_buses`: 버스 대수
- `n_morning`, `m_afternoon`: 오전/오후 각각 버스 한 대당 운행 횟수
- `morning_start`: 오전 각 버스 첫 회차 출발 시각의 하한(공통 목표)
- `afternoon_target_start`: 오후 목표 시작 시각(공통, 4.3절 참고)
- `day_end`, `mid_start`, `mid_end`: 4.4~4.6절 참고 (모두 선택적, 생략 가능)
- `headway_lo`, `headway_hi`: 배차간격 허용 범위(분)
- `rest_lo`, `rest_hi`: 휴게시간 허용 범위(분, 생략 시 자동계산)
- `tau_sm_default`, `tau_me_default`: 시작→중간, 중간→끝 구간의 기준
  이동시간(분)
- 각종 override 딕셔너리/리스트: lunch_break_overrides(버스별 점심시간
  범위), meal_break_overrides(버스+회차별 휴게시간 범위),
  occurrence_rest_overrides(회차 번호만으로 전체 버스 공통 휴게시간 범위),
  mid_origin_overrides(버스+회차의 중간출발 지정), travel_time_overrides
  (버스+회차+구간의 이동시간 직접 조작 델타), time_bands(슬랙/피크 규칙
  목록)

## 9. 실패(infeasible) 시 요구사항

모든 제약을 동시에 만족하는 스케줄이 존재하지 않을 수 있다. 이런 경우
시스템은:

- 단순히 "실패"라고만 하지 않고, **그나마 가장 제약 위반을 최소화한 참고용
  스케줄**을 함께 보여줘야 한다.
- **정확히 어떤 제약이, 얼마나(구체적인 수치로) 위반되는지** 사람이 읽을 수
  있는 형태로 알려줘야 한다(예: "이 배차간격은 25~30분이어야 하는데 실제로는
  18.3분").
- 가능하다면 **무엇을 얼마나 조정하면 풀리는지**에 대한 자동 안내도 제공해야
  한다.
- 이 안내 과정에서, 여러 설정을 한꺼번에 완화해서 "풀렸다"고 판단한 뒤
  그중 실제로는 원인이 아닌 설정까지 "원인"으로 잘못 지목하는 일이 없어야
  한다 — 각 요인이 정말로 원인인지는 그 요인 하나만 독립적으로 완화했을 때도
  풀리는지로 확인해야 한다.

## 10. 도메인상 알아두어야 할 특성 (사실관계, 버그 아님)

이 문제를 풀다 보면 아래와 같은, 얼핏 보기엔 이상해 보이지만 실제로는
제약들의 상호작용으로 인해 나타나는 정당한 현상들이 있다. 참고로 남긴다
(해결 방식이 아니라 문제 자체의 성질임):

- 4.6절(중간정류장 목표 서비스 시작)로 인해 일부 버스를 중간출발로 앞당겨
  배치하면, 중간 정류장 배차간격 제약을 통해 **그 뒤를 잇는 일반(시작 출발)
  버스들의 출발 시각도 도미노처럼 밀릴 수 있다** — 이는 오류가 아니라 공유된
  배차간격 사슬의 자연스러운 결과다.
- 4.10절(슬랙타임)로 인해 특정 트립의 이동시간이 "짧아질 수 있게" 허용해도,
  그게 항상 전체 문제를 더 쉽게 만들어주는 것은 아니다. 같은 버스가 반복
  운행하는 주기의 "이동시간 + 휴게시간" 합계는 배차간격 수식에서 나오는
  특정 범위 안에 들어와야 하는데, 이동시간이 짧아지면 그 합계가 오히려 그
  범위보다 작아져서 못 풀리게 될 수도 있다(휴게시간 상한을 함께 넓혀주지
  않으면). 특히 현재 설정이 "배차간격이 요구하는 범위"와 "이동시간+휴게시간
  범위"가 서로 정확히 한 점에서만 겹치는(여유가 0인) 상태라면, 아주 작은
  변화로도 문제 자체가 안 풀리게 바뀔 수 있다.
- 4.3절(점심시간)의 기본 정책은 상한이 없을 뿐, 하한(일반 휴게시간 하한)은
  항상 지켜야 한다 — "쉬는 시간 없이 바로 이어서 출발"하는 조합은 기본
  정책에서도 나오면 안 된다.
- day_end, mid_end는 각각 독립적인 "상한"일 뿐이다. 실제로 그 시각보다
  훨씬 일찍 끝나는 것은 전혀 문제가 아니며 오히려 자연스럽다 — "정확히 그
  시각에 도착해야 한다"는 요구가 절대 아니다.

## 11. 참고용 예시 (실제 테스트에 쓰였던 값들)

문제를 구체적으로 상상하기 위한 참고용 수치 — 필수 요구사항은 아님:

- 버스 3~7대, 오전 3~4회/버스, 오후 3회/버스
- 오전 시작 05:00~06:00, 오후 목표 시작 12:00~13:00
- 배차간격 8~30분, 휴게시간 8~30분대
- 시작→중간 15~80분, 중간→끝 10~80분
- 하루 마감 23:00~24:00대, 중간정류장 마감 23:00~23:30대
- 중간정류장 목표 서비스 시작 05:30대
